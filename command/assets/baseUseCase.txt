import {ValidProperties} from "../../../../core/shared/decorators/generalDecorators";
import { I:modulenameRepository } from '../../domain/repositories/i:modulename.repository';
import {:nameDTO} from "./:name.dto";
import {FlowsEnum} from "../../../../core/shared/services/Enums";

const _flow=`/${FlowsEnum.HELLO}`

export class :nameCase {

  constructor(
    private readonly repository:I:modulenameRepository
  ) {}

  @ValidProperties(_flow)
  async execute(data: :nameDTO) {
    return new Promise((resolve) => {

      resolve(data)

    })
  }
}