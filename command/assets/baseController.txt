import {Request,Response} from "express";
import {:nameCase} from '../../application/:casenameCase/:name.case'
import {:nameDTO} from '../../application/:casenameCase/:name.dto'
import { :nameRepository } from '../../infrastructure/persistence/:name.repository';

export class :nameController {

  /**
   * Hello :nameController
   * @param req
   * @param res
   */
  async helloApp(req: Request,res: Response) {

    const _dto = new :nameDTO(req.body)
    const _int = new :nameCase(
      new :nameRepository()
    )
    _int.execute(_dto)
      .then((resp) => res.send(resp))
      .catch((e) => res.status(400).send(e))
  }

}